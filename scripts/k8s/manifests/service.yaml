apiVersion: v1
kind: Service
metadata:
  name: nova-ai
  labels:
    app: nova-ai
spec:
  type: ClusterIP
  selector:
    app: nova-ai
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
